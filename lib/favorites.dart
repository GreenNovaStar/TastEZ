import 'package:flutter/material.dart';
import 'home.dart';

Widget favorites() {

    return MaterialApp(
        debugShowCheckedModeBanner: false,
        home: Center(
        child: Text("Favorites"),
    )
    );
}